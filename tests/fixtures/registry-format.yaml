name: registry-test-bundle
version: 1.0.0

session:
  orchestrator:
    module: loop-streaming
    config:
      extended_thinking: true

providers:
  - module: provider-anthropic
    config:
      model: claude-sonnet-4-20250514

tools:
  - module: tool-filesystem
  - module: tool-bash
